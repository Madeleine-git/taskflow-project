{
  "scripts": {
    "build": "npm install -g tailwindcss && tailwindcss -i ./src/input.css -o ./output.css --minify",
    "dev": "npx tailwindcss -i ./src/input.css -o ./output.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}