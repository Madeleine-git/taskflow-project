{
  "version": 2,
  "buildCommand": "rm -rf node_modules package-lock.json && npm install && npx tailwindcss -i ./src/input.css -o ./output.css --minify",
  "outputDirectory": ".",
  "framework": null,
  "installCommand": "npm install"
}